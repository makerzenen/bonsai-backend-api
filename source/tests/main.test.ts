describe("buildExpress()", () => {
  it("should respond with an express app", () => {
  });
});

describe("buildApollo()", () => {
  it("should respond with an apollo server", () => {
  });
});

describe("buildServer()", () => {
  it("should respond with a combined express and apollo server", () => {
  });
});
