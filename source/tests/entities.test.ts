import TicketModel from "../entities/Ticket"
import { simulateTicketsAPI } from "./helpers"

describe("source/entities/Ticket.ts", () => {
  it("should create a valid ticket model", () => {
  	/*
  	const ticket = new TicketModel(marshalTicket(simulateTicketsAPI[1]))
    ticket.saveFields()
    */
  });
});
